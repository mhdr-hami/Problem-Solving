//NOT SUBMITTED
// MAIN IDEA:

int myfunc (vector <int> vec)
{
    int curmx = vec[0];
    int themx = vec[0];
    for(int i=1 ;i<vec.size(); i++)
    {
        curmx = max(curmx + vec[i] , vec[i]);
        themx = max(curmx , themx);
    }
    return themx;
}
